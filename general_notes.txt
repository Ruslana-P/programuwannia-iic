Commit 1 

 install python 
 python3 -m ensurepip --upgrade. // install pip in a Python environment.
 python3 -m venv tutorial-env // create virtual eviroment
 source tutorial-env/bin/activate //activate virtual eviroment
 python -m pip install Django // install Django
python -m django --version. // check that django installed

--------------- writing your first app -------------------
 mkdir djangotutorial //create folder
 django-admin startproject mysite djangotutorial // start project in misite in folder djangotutorial

project structure 
- manage.py: A command-line utility that lets you interact with this Django project 
- mysite/: A directory that is the actual Python package 
- mysite/__init__.py: An empty file that tells Python that this directory should be considered a Python package
- mysite/settings.py: Settings/configuration for this Django project
- mysite/urls.py: The URL declarations for this Django project; a “table of contents” of your Django-powered site. 
- mysite/asgi.py: An entry-point for ASGI-compatible web servers to serve your project.
- mysite/wsgi.py: An entry-point for WSGI-compatible web servers to serve your project. 

python manage.py runserver // run application in  http://127.0.0.1:8000/
python manage.py startapp polls // create app 


commit 2 
python manage.py migrate // command looks at the INSTALLED_APPS setting and creates any necessary database tables 

Default db for Django is for SQLite, 
Interactive command-line client for SQLite - sqlite3.
sqlite3 db.sqlite3 - acces to SQlite  (need to be in djangotutorial folder)
.tables - view the tables
.quit - end

python manage.py makemigrations polls	//Blueprint: Scans models.py for changes and creates a migration file (e.g., 0001_initial.py).
python manage.py sqlmigrate polls 0001	// Inspect: Translates migration file (0001) into raw SQL statements and prints them (does NOT run them).
python manage.py migrate	// Execute: Applies all unapplied migration files to the database, creating or modifying tables.

 python manage.py shell //use this nstead of python because it automaticly imports models
 exit()


------- Python shell interaction commands --------
Question.objects.all()
Question.objects.filter(id=1)
Question.objects.filter(question_text__startswith="What")

Get the question that was published this year.
>>> from django.utils import timezone
>>> current_year = timezone.now().year
>>> Question.objects.get(pub_date__year=current_year)

# Lookup by a primary key
>>> Question.objects.get(pk=1)
------------------------------------------



python manage.py createsuperuser // create super user
username - admin 
password admin_password



--- unit tests in python ----
python manage.py test polls // run tests


-- start app
be in - /path/to/programuwannia-iic
source tutorial-env/bin/activate

go to 
cd djangotutorial/
python manage.py runserver


---- debugging 
python -m pip install django-debug-toolbar // install tool in active env 